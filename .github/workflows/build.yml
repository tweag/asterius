name: build

on:
  - push
  - pull_request

jobs:

  build:
    name: build
    runs-on: ubuntu-18.04
    container:
      image: terrorjack/asterius:dev-200517
    steps:

      - name: checkout
        uses: actions/checkout@v2

      - name: boot
        run: |
          .github/workflows/boot.sh
