name: build

on:
  - push
  - pull_request

jobs:

  build:
    name: build
    runs-on: ubuntu-18.04
    steps:

      - name: checkout
        uses: actions/checkout@v2

      - name: boot
        run: |
          podman run \
            --network host \
            --pull \
            --rm \
            --volume $(pwd):/workspaces/asterius \
            --workdir /workspaces/asterius \
            terrorjack/asterius:dev-200517 \
            .github/workflows/boot.sh
